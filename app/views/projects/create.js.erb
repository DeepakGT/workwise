<% if @project.errors.any? %>
  message = "<%= @project.errors.full_messages.first %>"
  Swal.fire({
    title: 'Error!',
    text: message,
    icon: 'error',
    confirmButtonText: 'Cool'
  })
<% else %>
  location.reload();
<% end %>
