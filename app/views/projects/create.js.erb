<% if @project.errors.any? %>
  message = "<%= @project.errors.full_messages.first.html_safe %>"
  Swal.fire({
    title: 'Error!',
    text: message,
    icon: 'error',
    confirmButtonText: 'Cool'
  })
<% else %>
  location.reload();
<% end %>
